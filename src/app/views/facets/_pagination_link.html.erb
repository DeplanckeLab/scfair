<turbo-frame id="<%= facet_pagination_frame_id(category) %>">
  <% if pagination&.dig(:has_more) %>
    <%= link_to_next_facet_page(category, pagination, colors: colors) %>
  <% end %>
</turbo-frame>
