<% response_frame_id = @frame_id.presence || dom_id_for_nodes(@category, @parent_id) %>
<turbo-frame id="<%= response_frame_id %>">
  <%= render "facets/tree_nodes",
             category: @category,
             parent_id: @parent_id,
             nodes: @nodes,
             level: @level,
             colors: @colors %>
</turbo-frame>
