<turbo-frame id="failed_datasets">
  <div class="mt-12 mb-4">
    <div class="bg-white rounded-lg shadow">
      <div class="p-4">
        <h3 class="text-lg font-bold text-center mb-4">
          Failed Datasets – <%= @source.name %> (<%= @datasets.length %>)
        </h3>

        <div class="space-y-3">
          <%= render partial: "datasets/dataset", collection: @datasets, as: :dataset %>
        </div>
      </div>
    </div>
  </div>
</turbo-frame>
